name: SSH

on:
  # workflow_call:
  #  inputs:
  #    env: 
  #     default: "s"
  #     required:  true
  #     type: string
  #     description: ''
 push:

jobs:
  # upload:
  #   name : Run upload artifact
  #   uses:  RTC12-Test/repo3/.github/workflows/check.yaml@main
  #   with: 
  #    env: ${{inputs.env }}

  ssh:
    runs-on: self-hosted
    # needs: ['upload']
    steps:
      - uses: actions/checkout@v4
      - name: Copy files via SSH
        uses: appleboy/scp-action@v1
        with:
          host: "192.168.122.220"
          username: "ghost"
          password: "as"
          port: 22
          source: "!hello/asd,hello/hello.txt"
          target: "/home/ghost/testssh"
