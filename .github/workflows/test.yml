name: test
on:
 push:
   push:
    branches: ["main"]

  # workflow_call:
  #   inputs:
  #     github_pat:
  #       description: 'aws service'
  #       type: string
  #       required: true
  #     task_definition:
  #       type: string
  #       description: 'aws task'
  #       required: true
  #     cluster:
  #       description: 'aws cluster'
  #       type: string
  #       required: true

permissions:
  id-token: write   # This is required for requesting the JWT
  contents: read  
  
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: create file 
        run: echo "hello world!" > ${{ runner.temp }} 
      - name: ls
        run: ls ${{ runner.temp }}
  job2:
    runs-on: ubuntu-latest
    needs: ["job1"]
    steps:
      - uses: actions/checkout@v4
      - name: ls
        run: ls ${{ runner.temp }}
  
       
           
